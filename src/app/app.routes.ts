import { Routes } from '@angular/router';
import { SeatingComponent } from './seating/seating/seating.component';

export const routes: Routes = [
    {path: 'hanchan', component: SeatingComponent }
];
